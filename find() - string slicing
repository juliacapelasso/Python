// Write code using find() and string slicing (see section 6.10) to extract the number at the end of the line below. 
Convert the extracted value to a floating point number and print it out.
//

text = "X-DSPAM-Confidence:    0.8475"
position1 = int(text.find('0'))
#isso me da a posição que os : estao, a posicao 18
#print(x)
position2 = int(text.find('5'))
#print(position2)
#y = position2 - position1
#print(y)
#y é a distância entre os dois pontos ao número que o ex quer

result = text[position1 : position2 + 1 ]
convv = float(result)
print(convv)
